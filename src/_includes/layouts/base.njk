<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ title or "Site" }}</title>
  <style>body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;max-width:800px;margin:40px auto;padding:0 16px;line-height:1.55}nav a{margin-right:12px}.muted{opacity:.7;font-size:.9em}</style>
</head>
<body>
  <nav>
    <a href="{{ '/' | url }}">Accueil</a>
    <a href="{{ '/posts/' | url }}">Posts</a>
  </nav>
  <main>{{ content | safe }}</main>
</body>
</html>
