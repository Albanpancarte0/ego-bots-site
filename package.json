{
  "name": "site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0"
}
    export default {
  pathPrefix: "/<ton-repo>/",   // remplace par le nom exact du repo
  dir: { input: ".", output: "_site" } // garde la sortie par défaut
  }
}
